_schema-version: '2.1'
ID: dcmhub_roro_m30
version: 0.0.1
#--- Template History ---#
# Version ; Comments
# 0.0.1   ; no rank views, extendet log out idle time
#
#------------------------#

modules:
  - name: dcmhub_roro_m30-db
    type: hdb
    path: db
    requires:
      - name: dcmhub_roro_m30-hdi-container
      - name: cross-container-service
        group: SERVICE_REPLACEMENTS
        properties:
          key: ServiceName_1
          service: '~{the-service-name}'      
  - name: xsjs_roro_m30
    type: nodejs
    path: xsjs_roro_m30
    properties:
      SESSION_TIMEOUT: 60
      INCOMING_CONNECTION_TIMEOUT: 3600000
    provides:
      - name: xsjs_api
        properties:
          url: '${default-url}'
    requires:
      - name: dcmhub_roro_m30-hdi-container
      - name: dcmhub_roro_m30-db
      - name: dcmhub_roro_m30-uaa
      - name: dcmhub_roro_m30-fs      
  - name: tool_roro_m30
    type: html5
    path: tool_roro_m30
    requires:
      - name: flp-tool_roro_m30-client
      - name: dcmhub_roro_m30-uaa
      - name: dcmhub_roro_m30-ui5
      - name: xsjs_api
        group: destinations
        properties:
          name: xsjs_api
          url: '~{url}'
          forwardAuthToken: true
          timeout: 900000
    properties:
      SESSION_TIMEOUT: 60
      INCOMING_CONNECTION_TIMEOUT: 3600000    
      CORS:
        - uriPattern: .
          allowedMethods:
            - GET
            - POST
          allowedOrigin:
            - host: '*'
      SAP_JWT_TRUST_ACL:
        - clientid: '*'
          identityzone: '*'          

  - name: webjava_roro_m30
    type: java
    path: webjava_roro_m30
    parameters:
       memory: 512M
    provides:
     - name: webjava_roro_m30_api
       properties:
          url: ${default-url}
    requires:
     - name: dcmhub_roro_m30-hdi-container
       properties:
          JBP_CONFIG_RESOURCE_CONFIGURATION: '[tomcat/webapps/ROOT/META-INF/context.xml:
             {"service_name_for_DefaultDB" : "~{hdi-container-name}"}]'

resources:
  - name: dcmhub_roro_m30-hdi-container
    type: com.sap.xs.hdi-container
    properties:
      hdi-container-name: '${service-name}'
  - name: cross-container-service
    type: org.cloudfoundry.existing-service
    parameters:
      service-name: DCMHUB-RORO-M30
    properties:
      the-service-name: '${service-name}'
  - name: dcmhub_roro_m30-ui5
    type: org.cloudfoundry.managed-service
    parameters:
      service: sapui5_sb
      service-plan: sapui5-1.71
  - name: dcmhub_roro_m30-uaa
    type: org.cloudfoundry.existing-service
    parameters:
      service-name: portal-uaa-dcmhubflp
  - name: flp-tool_roro_m30-client
    type: com.sap.portal.site-content
    parameters:
      config:
        siteId: dcmhubflp
  - name: dcmhub_roro_m30-fs
    type: com.sap.xs.fs
    parameters:
      service-name: FILEDIR_RORO_M30
